# Notes pour Claude Code

## Serveur de développement

**⚠️ IMPORTANT : Ne JAMAIS lancer `npm run dev`**

L'utilisateur a toujours un serveur dev qui tourne sur le port 3000 en parallèle des sessions Claude Code.

**À FAIRE :**
- ✅ Vérifier le build avec `npm run build` si nécessaire
- ❌ Ne PAS lancer `npm run dev` ou tout autre serveur en arrière-plan

**Serveur actif :**
- Port : 3000
- URL : http://localhost:3000
- État : Toujours en cours d'exécution en parallèle
